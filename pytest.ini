[pytest]
# Host-side unit tests for the plant-autonomy hub (P2-13).
# Firmware is validated on-device; these cover the Python/SQL logic that can run
# on any host with no hardware, no network, and no live database.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
