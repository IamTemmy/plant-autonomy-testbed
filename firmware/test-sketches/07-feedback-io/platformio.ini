; Plant Autonomy Testbed — Phase 1 component test: User-feedback subsystem
;
; Built iteratively across commits:
;   Pass 1 (this commit): LEDs only — state-pattern blink test
;   Pass 2: + buttons (state transitions on press)
;   Pass 3: + buzzer (audible alarm in CRITICAL state)
;
; Final form validates all 6 user-feedback components together.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
