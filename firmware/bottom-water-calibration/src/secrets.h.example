#pragma once
// Template for secrets.h. Copy this file to secrets.h and fill in real values,
// or just copy your existing integrated-firmware secrets.h — the field names
// are identical.
//   cp secrets.h.example secrets.h
// secrets.h is gitignored and must NEVER be committed with real credentials.

static constexpr char WIFI_SSID[]     = "YOUR_WIFI_SSID";
static constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";

static constexpr char MQTT_USER[]     = "YOUR_MQTT_USER";
static constexpr char MQTT_PASSWORD[] = "YOUR_MQTT_PASSWORD";
